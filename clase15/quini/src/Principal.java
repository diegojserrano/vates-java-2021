import java.util.*;

public class Principal {

    public static void main(String[] args) {
        Map<Integer, Apuesta> m = new TreeMap<>();

        m.put(53930,new Apuesta(new HashSet<>()));

        Set<Integer> sorteados = new HashSet<>();
        sorteados.add(Random)

        for(Apuesta a: m.values())
            if (a.acerto4(sorteados))
                System.out.println(a);

    }
}
